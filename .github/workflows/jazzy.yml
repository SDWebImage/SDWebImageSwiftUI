name: Swift

on:
  push:
    # Push target branch toggle action.
    branches: [ action ]

jobs:
  # Job named build.
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@1.5.0
    - name: Install jazzy.
      run: gem install jazzy
